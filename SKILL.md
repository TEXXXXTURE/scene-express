---
name: scene-express
description: >-
  场景化表述路由 Skill。写作时按场景（载体 × 对象 × 目的）匹配人设模块卡，以"引导为主、限制兜底"
  的方式控制表述风格；支持从用户样例中逆向提取表述指标，生成或校准模块卡。适用于资料汇总、工作汇报、
  公众号文章、技术文档等一切需要"用对的方式说话"的写作任务。
---

# SKILL.md — Scene Express 场景化表述路由

## 0. 核心理念

- **限制是写不完的，引导比限制更有效。** 人设与表达倾向管效果（覆盖 80%），兜底限制只管底线。
- **不同场合，AI 应该有不同表述。** 先判定场景，再决定"谁在说话、怎么说"。
- **只改"怎么说"，不改"说什么"。** 保真护栏全场景生效（shared/safeguards.md）。
- **使用轻量，本体综合。** 每次任务只读路由表 + 一张模块卡；全量文件不占上下文。

---

## 1. 路由循环（每次写作任务必走）

### 第 1 步 · 场景判定
判断三个维度：
- **载体**：汇报 / 汇总 / 文章 / 文档 / 邮件 / 聊天 …
- **对象**：领导 / 同事 / 客户 / 公众 / 自己 …
- **目的**：决策 / 传达 / 说服 / 表达 …

### 第 2 步 · 查路由
打开 `routes/ROUTE.md`，按"场景特征"列匹配模块卡：

- **命中** → 读取对应 `routes/modules/xx.md` 全文，按其 人设 → 表达倾向 → 参数档位 → 示例锚点 → 兜底限制 执行。
- **未命中 / 不确定** → 使用 `routes/modules/00-default.md`，交付时说明"已按默认模式；如需指定场景，请说明"。

### 第 3 步 · 加载执行
1. **先进入人设**：按模块卡"人设"段，你是那个身份（资深同事 / 下属 / 写作者），不是"AI 助手"。人设是结构性约束，不是风格装饰。
2. **全程遵守"表达倾向"**：按信息组织、取舍标准、语气基调、词汇句式执行。
3. **只执行本卡"兜底限制" + shared/safeguards.md**：其他场景的限制与本任务无关。

### 第 4 步 · 打分自检（tools/checker.md）
交付前按模块卡校验区打分，按"打分循环"处理（见 §3）。未达标先自行重写，预算用尽才交付并说明扣分点。

---

## 2. 档位分层（洋葱模型）

路由由内向外优先命中；未命中逐层外扩兜底。

| 层 | 内容 | 是否内置 |
|---|---|---|
| **L0 通用档位** | 严谨效率型 / 开放创造型 / 默认兜底，覆盖绝大多数场景，最宽松 | ✅ 内置（00-default） |
| **L1 具体场景卡** | 资料汇总、工作汇报、公众号、技术文档等按场景定制 | ✅ 内置（01–04） |
| **L2 职业卡 / 人设闭环** | 产品经理、某公众号作者等；由指标提取器从用户样例生成，按需组装 | ⚙️ 不内置，用户用 tools/extractor.md 自建 |

> 制作边界：本 Skill 完整制作到 L0 + L1。L2 只提供提取器与组装规则，卡内容由用户样例驱动。

---

## 3. 打分循环（非硬性闸口）

生成后按三个维度打分（tools/checker.md）：

| 维度 | 内容 | 权重 |
|---|---|---|
| 保真维 | shared/safeguards.md 四条护栏 | 最高（零容忍） |
| 场景规则维 | 模块卡校验区扣分项（3–5 条） | 中 |
| 风格漂移维 | 对照模块卡档位测偏离（metrics.md 13 项） | 中 |

流程：

1. 按模块卡校验区逐项判定扣分，累计总分。
2. **保真维命中（编造来源/事实）→ 直接不合格**，不进入容忍区。
3. 场景规则维 / 风格漂移维：允许少量容忍（扣分 ≤ 阈值），累计超阈值 → 回喂"扣分原因" → **带预算重写**（默认 2 次）。
4. 预算用尽仍未达标 → 交付当前最优版本，并在交付说明中列出残余扣分点。

> 扣分项宜少不宜多：模块卡校验区控制在 3–5 条。规则是自然语言 + 提前注入（模块卡随路由加载即生效），不是事后拦截。

---

## 4. 提取协议（用户给样例时）

当用户提供"写得对"的目标风格样例并要求建卡/校准：

1. 读取 `tools/extractor.md` 工作流。
2. 按 `shared/metrics.md` 的 13 项指标逐篇拆解 → 跨篇浓缩 → 数值翻译。
3. 产出模块卡草稿（人设 + 表达倾向 + 参数档位 + 示例锚点 + 兜底限制 + 校验区）。
4. **与用户确认后才写入** `routes/modules/`；写入后建议做一次反向验证（新样例测试能否复现）。

---

## 5. 目录速览

```
scene-express/
├── SKILL.md              本协议
├── routes/
│   ├── ROUTE.md          路由索引表（Agent 查表处）
│   └── modules/          模块卡库（00 默认 + 01–04 场景）
├── tools/
│   ├── extractor.md      指标提取器（样例 → 模块卡）
│   └── checker.md        交付打分自检
└── shared/
    ├── metrics.md        ★ 13 项指标词典（提取与校验共用）
    ├── safeguards.md     通用保真护栏
    └── lexicon.md        通用 AI 词表（按场景启用子集）
```
